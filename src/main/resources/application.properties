spring.jpa.hibernate.ddl-auto=update
spring.datasource.url=${DB_URL}
spring.datasource.username=${DB_USERNAME}
spring.datasource.password=${DB_PASSWORD}
spring.jpa.database-platform=org.hibernate.dialect.MySQL8Dialect
azure.storage.connection-string=${AZURE_CONNECTION_STRING}

spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=yoimarnova123@gmail.com
#contraseña a nivel de aplicación
spring.mail.password=vdhf kcqb kxnm rsmf
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true

#Nivel global de log
logging.level.root=DEBUG

#Nivel de log para Spring Web (para capturar peticiones)
logging.level.org.springframework.web=DEBUG

#Nivel de log para Spring Security (útil para ver detalles de autorización)
logging.level.org.springframework.security=DEBUG

#Nivel de log opcional, es mucho más alto para Spring security
logging.level.org.springframework.security.web.firewall=DEBUG

#Nivel de log para Hibernate (opcional)
logging.level.org.hibernate=INFO

# Niveles de log para depuración de WebSockets y Spring MVC
logging.level.org.springframework.messaging=DEBUG
logging.level.org.springframework.web.socket=DEBUG
logging.level.org.springframework.web.servlet=DEBUG

# Nivel de log para paquetes específicos
logging.level.com.germogli.backend.community.message=DEBUG
logging.level.com.germogli.backend.common.config=DEBUG